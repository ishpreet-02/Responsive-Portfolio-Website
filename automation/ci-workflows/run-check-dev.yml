name: Developer Branch CI

on:
  push:
    branches:
      - Developer

jobs:
  developer-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Developer Says Hello
        run: echo "✅ Developer branch workflow running successfully!"
